#include <stdio.h>
#include <conio.h>

int main()
{
    int n1, i = 1;
    printf("Enter Number for printing the table: ");
    scanf("%d", &n1);
    while (i <= 10)
    {
        printf("%d x %d = %d\n", n1, i, n1 * i);
        i++;
    }
    getch();
} Any key and then Enter To Kill Process. ");
    scanf("%1s", &ch);
    return 0;
}